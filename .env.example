DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
# Local dev only - use a long random secret in production
DJANGO_SECRET_KEY=dev-secret-key-change-in-production

# Production only:
# DJANGO_DEBUG=false
# DJANGO_ALLOWED_HOSTS=yourdomain.com
# DJANGO_CSRF_TRUSTED_ORIGINS=https://yourdomain.com
# DJANGO_SECURE_SSL_REDIRECT=true
POSTGRES_USER=postgres
# Local dev only - use a strong password in production (avoid # @ : %)
POSTGRES_PASSWORD=devpass
DATABASE_URL=postgres://postgres:devpass@db:5432/construction_reports
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Podman/container stack (infra/podman-compose.yml)
APP_IMAGE=ghcr.io/rupret007/constructiondaily:feature-latest
APP_PORT=8000
# Optional: override for deploy/poll scripts
# DEPLOY_PATH=/opt/constructiondaily
# COMPOSE_FILE=/opt/constructiondaily/infra/podman-compose.yml

VITE_API_BASE=/api
